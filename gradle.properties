org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g
org.gradle.parallel=true
org.gradle.caching=true

# android.useAndroidX now defaults to true in AGP 9, so it is not set here.

kotlin.code.style=official
