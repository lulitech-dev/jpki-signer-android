.gradle/
build/
local.properties
*.iml
.idea/
.kotlin/
captures/
*.apk
*.aab
*.keystore
!debug.keystore

# Local reference implementation; a clone with its own .git.
jpki-pdf-signer/

# Local working notes, deliberately untracked. The subset the source comments
# cite is tracked in DESIGN.md instead.
PLAN.md
CLAUDE.md

# Scratch space for real signed documents under diagnosis. These carry a
# 署名用証明書, so they must never be committed.
tmp/
